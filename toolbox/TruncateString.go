package toolbox

/* func TruncateStr(s string, len int) string {
	if len <= 0 {
		return ""
	}
	truncated := ""
	count := 0
	for _, char := range s {
		truncated += string(char)
		count++
		if count >= len {
			break
		}
	}
	return truncated
} */
